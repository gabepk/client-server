#!/usr/bin/perl

if ("http://www.google.com" =~ m{^(([^:/?#]+):)?(//([^/?#]*))?([^?#]*)(\?([^#]*))?(#(.*))?} ) {
  my($host, $path) = ($4, $5);
  print "$host => $path";
  print "It matches\n";
}
else {
  print "It doesn't match\n";
}
